# AI 测试驱动开发执行规范

> 版本：1.0
>
> 用途：复制到任意项目中，作为 AI 编码助手的 TDD 工作规则。
>
> 适用范围：新增功能、缺陷修复、行为变更；纯重构和文档任务按本文的特殊流程执行。

## 0. 放置与启用

1. 将本文件复制到目标项目根目录。
2. 保留文件名时，在该项目的 AI 入口规则中加入：
   `执行代码任务前，必须完整阅读并遵守 AI_TDD_EXECUTION_RULES.md。`
3. 如果所用 AI 只自动读取固定文件名，可将本文合并到它支持的仓库规则文件中，例如
   `AGENTS.md`、`CLAUDE.md` 或其他工具约定的文件。
4. 目标项目已有规则文件时，只追加或引用本文，不得直接覆盖原规则。
5. 完成第 3 节“项目适配区”后，再让 AI 开始第一个代码任务。

只有当 AI 工具确实会读取本文件或入口规则明确引用本文件时，本规范才会生效。

## 1. 规范目标

本规范要求 AI 以可观察的业务行为为起点，通过短周期的
**Red（失败）→ Green（通过）→ Refactor（重构）** 完成交付。

TDD 在本文中不是“实现后补测试”，而是以下可验证事实：

1. AI 先向开发者提交测试方案。
2. 测试方案经过开发者明确审核同意。
3. 实现前先存在一个描述目标行为的测试。
4. AI 实际运行该测试，并确认它因目标行为尚未实现而失败。
5. AI 只编写足以让当前测试通过的生产代码。
6. AI 实际运行测试并确认通过。
7. AI 在测试保护下重构，并运行适当范围的回归测试。

如果没有观察到有效的 Red，AI 不得声称本次工作遵循了完整 TDD。
如果测试未经开发者审核同意，AI 不得新增、修改、删除、跳过或禁用该测试。

## 2. 规则关键字

- **必须（MUST）**：不可跳过；跳过即不符合本规范。
- **应该（SHOULD）**：默认执行；只有明确、可说明的理由才能偏离。
- **不得（MUST NOT）**：禁止行为。
- **可以（MAY）**：根据项目情况选择。

当本文与用户的明确要求、仓库内更具体的规则或安全要求冲突时，按以下优先级执行：

1. 用户当前明确要求与安全要求；
2. 当前仓库中作用域更具体的规则；
3. 本规范；
4. AI 的默认习惯。

发生冲突时，AI 必须说明采用了哪条更高优先级规则。

## 3. 项目适配区

将本文件复制到新项目后，维护者或 AI 必须先根据仓库实际情况填写本节。不得猜测不存在的命令。

```text
语言/框架：
单元测试目录：
集成测试目录：
测试数据工厂目录：
数据库迁移目录：

快速测试命令 TEST_FAST：
指定测试文件命令 TEST_FILE：
指定测试用例命令 TEST_CASE：
模块回归命令 TEST_MODULE：
完整测试命令 TEST_ALL：
静态检查命令 CHECK_STATIC：
格式化命令 CHECK_FORMAT：

默认排除的慢速/在线测试标签：
运行测试所需的本地服务或容器：
项目统一的测试命名风格：
项目统一的响应、异常和权限约定：
```

如果适配区尚未填写，AI 必须通过构建文件、CI 配置、现有测试和仓库文档发现真实命令，并在开始实现前补全本节或在任务记录中明确列出。AI 不得仅因 README 中写有某条命令就假定它仍然有效；应检查脚本或构建目标确实存在。

## 4. 开始任务前的强制检查

AI 修改代码前必须完成以下检查：

1. 阅读仓库级与目标目录内的指导文件。
2. 检查工作区状态，识别用户已有的未提交修改，不覆盖、不回滚无关改动。
3. 找到与需求最接近的生产代码、测试、测试工厂和公共测试基类。
4. 从构建文件或 CI 配置确认测试命令。
5. 运行与目标模块最相关的现有测试，建立基线。
6. 将需求拆成一个或多个可独立验证的行为切片。

基线测试失败时：

- AI 必须判断失败是否由当前改动前已存在的问题造成。
- 与任务无关的既有失败必须记录，不得顺手扩大范围修复。
- 既有失败不妨碍目标测试独立验证时，可以继续，但最终报告必须明确区分既有失败和本次结果。
- 无法区分失败来源时，AI 必须先诊断，不得进入 Green 阶段。

## 5. 先把需求变成行为

每个行为切片必须能写成以下句式之一：

```text
Given <前置状态>
When  <用户或系统执行某个动作>
Then  <可从外部观察的结果>
```

或：

```text
<主体> 在 <条件> 下，可以/不可以 <行为>，并得到 <结果>
```

### 5.1 最小场景矩阵

对每个接口、用例或业务动作，AI 至少检查以下场景是否适用：

| 场景 | 要回答的问题 |
|---|---|
| 成功路径 | 合法输入时，对外结果和持久化副作用是什么？ |
| 身份认证 | 未登录调用时会发生什么？ |
| 权限边界 | 已登录但无权操作其他资源时会发生什么？ |
| 输入校验 | 空值、格式错误、边界值如何处理？ |
| 资源状态 | 资源不存在、已删除、未发布或状态不允许时如何处理？ |
| 重复操作 | 重复提交是否幂等，是否产生重复数据？ |
| 并发 | 并发写入是否可能破坏唯一性或计数？ |
| 外部失败 | 数据库、队列、邮件、第三方接口失败时如何处理？ |

AI 不必机械地为每行都写测试，但必须覆盖与本次行为有关的主要成功路径和风险最高的失败路径。

### 5.2 开发者审核门禁

测试用例属于开发者审核资产。AI 必须先提交测试方案，获得开发者明确同意后，才能编辑任何测试代码。

本门禁覆盖测试方法、测试文件、断言、快照、测试数据工厂、Fixture、Seeder、Mock/Fake、公共测试基类、测试标签以及仅供测试使用的配置。纯格式化如果会改写这些文件，也必须先获得同意。

测试方案至少包含：

```text
目标行为：
测试层级：单元 / 集成 / 端到端
测试名称：
Given：
When：
Then：
关键断言：
需要的测试数据或替身：
预计修改或新增的测试文件：
目标测试命令：
```

开发者的同意必须针对可识别的测试方案或测试变更，例如“同意上述 3 个用例”“按该方案编写测试”或“同意修改 `<测试文件>`”。仅提出功能需求、要求修复缺陷或要求遵守 TDD，不自动等同于批准具体测试用例。

在获得同意之前，AI：

- 可以阅读和分析现有测试；
- 可以用文本或补丁草案展示建议测试；
- 可以运行现有测试建立基线；
- 不得写入、修改、删除、重命名、跳过或禁用任何测试文件或测试用例；
- 不得先实现生产代码以绕过测试审核门禁。

测试获得同意并写入后，在当前 Red–Green 循环中视为不可变契约。除非开发者再次明确同意，AI 不得为了让测试通过而调整其输入、断言、夹具、超时、标签或执行条件。

如果 AI 认为现有测试或已审核测试不合理，必须暂停相关实现并反馈开发者，至少说明：

1. 哪个测试或断言存在问题；
2. 复现命令和实际结果；
3. 为什么它与需求、业务规则、其他测试或真实系统行为冲突；
4. 建议如何修改；
5. 修改测试可能影响哪些既有行为。

开发者决定之前，AI 不得擅自修改测试，也不得通过扭曲生产实现来迎合已知不合理的测试。

### 5.3 切片原则

- 一个切片只引入一个主要行为变化。
- 优先选择能从入口到可观察结果形成闭环的垂直切片。
- 大需求必须按“最简单成功路径 → 校验 → 权限 → 异常/边界”逐步推进。
- 每次只让一个新行为进入 Red；不得一次堆积大量失败测试后再统一实现。

## 6. 选择正确的测试层级

测试层级由行为风险决定，不由代码目录机械决定。

### 6.1 单元测试

适用于：

- 业务策略、权限判断、状态转换；
- 纯计算、格式化、解析和校验；
- 服务层的重要分支；
- 错误处理和边界条件；
- 可以通过稳定接口隔离依赖的逻辑。

要求：

- 运行快、结果确定、不依赖网络和真实时钟；
- 只 mock 被测边界之外的协作者；
- 优先断言返回值、状态变化或领域事件，而不是实现细节；
- 不为覆盖率而 mock 被测对象本身。

### 6.2 集成测试

适用于：

- HTTP/RPC 入口到业务层、持久化层的完整行为；
- ORM 映射、查询、事务和数据库约束；
- 安全过滤器、序列化、参数校验和统一错误响应；
- 消息发布/消费、缓存、邮件、文件存储等组件协作；
- 框架配置是否真正生效。

要求：

- 使用项目公共测试基类或测试夹具；
- 尽可能使用与生产一致的数据库/队列/缓存实现，可通过容器提供；
- 第三方公网服务默认使用可控替身，不让默认测试依赖公网；
- 同时验证协议结果和关键副作用，例如响应 + 数据库状态；
- 每个测试可独立运行，不能依赖测试执行顺序。

### 6.3 端到端测试

仅在跨进程、跨系统或关键用户旅程无法由较低层测试可靠覆盖时使用。端到端测试应少而稳定，不能代替单元和集成测试。

### 6.4 测试层级决策

```text
纯逻辑或单类规则？                    → 单元测试
依赖框架装配、数据库、事务或协议？     → 集成测试
缺陷曾穿过多个层级才暴露？             → 回归集成测试，必要时补单元测试
跨服务关键流程？                       → 少量端到端测试 + 各服务低层测试
```

一个功能可以同时需要集成测试和单元测试：集成测试描述用户可见行为，单元测试精确覆盖复杂业务分支。

## 7. Red：先写一个会正确失败的测试

### 7.1 Red 阶段步骤

1. 向开发者提交当前行为的测试方案。
2. 等待开发者明确审核同意。
3. 只按已审核方案添加当前行为所需的测试、测试数据构造和最少的测试基础设施。
4. 测试名必须描述业务行为，而不是实现方法名。
5. 使用 Given–When–Then 或 Arrange–Act–Assert 清晰分段。
6. 运行最小范围的目标测试。
7. 阅读完整失败信息，确认失败原因。
8. 记录开发者批准、Red 使用的命令和核心失败原因。

### 7.2 有效 Red 的判定

只有同时满足以下条件，Red 才有效：

- 失败由目标行为尚未实现或缺陷仍可复现导致；
- 测试确实执行到了目标断言或预期的编译边界；
- 不是语法错误、错误导入、测试数据错误、环境故障或无关测试失败；
- 测试在错误实现下失败，在正确实现下应通过。

优先让 Red 表现为清晰的断言失败。因新增公开契约尚不存在而产生的编译失败可以作为短暂 Red，但 AI 必须确认缺失符号正是需求的一部分。

以下情况不是有效 Red：

- Docker、数据库或依赖服务未启动；
- 网络、权限、磁盘空间或依赖下载失败；
- 测试选择器写错，实际运行了 0 个测试；
- 测试本身空指针或夹具损坏；
- 与需求无关的旧测试失败；
- 测试第一次运行就通过。

如果新测试第一次运行就通过，AI 必须调查：

1. 行为是否已经存在；
2. 断言是否过弱或命中了错误对象；
3. 测试是否真正执行；
4. 需求是否实际是重构或已有行为的表述。

在产生有效 Red 前，不得开始针对该行为编写生产实现。

## 8. Green：用最小改动让测试通过

### 8.1 Green 阶段步骤

1. 只修改实现当前行为必需的生产代码。
2. 不预先实现后续切片，不顺手做无关清理。
3. 先运行目标测试，直到它通过。
4. 再运行受影响模块的测试。
5. 记录 Green 使用的命令和结果。

“最小实现”不等于使用明显错误的硬编码。实现可以简单，但必须对当前已知输入域成立，并遵守安全、数据一致性和项目架构约束。

### 8.2 Green 阶段禁止事项

AI 不得为了变绿而：

- 未经开发者明确同意，新增、修改、删除、重命名、跳过或禁用测试；
- 删除、跳过、禁用或降低测试断言；
- 把失败测试改成只验证“不抛异常”；
- 在测试中复制生产算法，使两者共同犯错；
- 给生产代码加入仅供测试使用的分支；
- 用无限重试、任意延长超时或固定休眠掩盖竞态；
- mock 掉本来要验证的核心行为；
- 改变无关公共接口或大范围重写代码；
- 吞掉异常或返回伪造成功结果。

如果测试暴露出需求矛盾，AI 必须停下来说明矛盾，不得自行选择一个会改变产品语义的答案。

## 9. Refactor：只改结构，不改行为

测试变绿后，AI 必须检查是否需要以下重构：

- 消除重复；
- 改善命名；
- 提取业务策略、测试工厂或公共夹具；
- 缩小方法和类的职责；
- 将基础设施细节移到边界；
- 清理仅为过测试产生的临时代码。

重构规则：

1. 重构前测试必须为 Green。
2. 每次进行小步结构调整。
3. 重构期间不得新增产品行为。
4. 每个有风险的调整后运行目标测试。
5. 完成后运行模块回归和适当范围的完整回归。

如果重构需要改变行为，必须开启新的 Red–Green–Refactor 循环。

## 10. 不同任务类型的执行方式

### 10.1 新增功能

严格执行：

```text
行为拆分 → 提交测试方案 → 开发者审核 → 测试 Red
→ 最小实现 Green → 重构 → 模块/全量回归
```

### 10.2 缺陷修复

必须先写能稳定复现缺陷的回归测试：

```text
提交回归测试方案 → 开发者审核 → 复现缺陷（Red）
→ 最小修复（Green）→ 补充邻近边界 → 回归
```

回归测试应从用户可见症状入手。若缺陷根因位于复杂策略中，可以再补一个更小的单元测试用于定位。

### 10.3 行为变更

先明确旧行为和新行为。修改或新增测试使其因旧实现而失败，再实现新行为。不得只删除与新需求冲突的旧测试；应把它改写为新契约，除非该行为已明确废弃。

### 10.4 纯重构

纯重构不要求制造 Red，但必须：

1. 先运行现有测试并确认 Green；
2. 必要时先提交表征测试方案，获得开发者同意后再补充测试以锁定当前行为；
3. 只改变结构；
4. 重构后再次运行相同或更大范围测试。

AI 必须把这种工作称为“测试保护下的重构”，不得伪称完成了 Red–Green 循环。

### 10.5 文档、配置或生成代码

- 纯文档任务运行适用的链接、格式或示例校验，不强制业务测试 Red。
- 配置变更必须有能验证配置效果的测试或检查。
- 生成代码不得手工进行大范围修改；应修改生成源或配置并重新生成。
- 生成过程会覆盖文件时，AI 必须先确认不会破坏用户改动。

## 11. 数据库变更规范

涉及数据库结构时，行为测试仍然先行。推荐顺序：

1. 提交从 API、服务或仓储层观察新行为的测试方案。
2. 获得开发者明确同意后，写出失败测试。
3. 添加向前兼容的迁移脚本。
4. 更新或重新生成实体、Mapper/Repository。
5. 编写最小业务实现。
6. 运行集成测试，确认迁移、映射、约束和事务共同生效。

强制要求：

- 只新增迁移，不修改已在共享环境执行过的历史迁移；
- 迁移命名、顺序和回滚策略遵循当前项目约定；
- 数据完整性规则应尽可能由数据库约束和业务测试双重保护；
- 测试不能依赖开发者本机已有数据库状态；
- 清理数据时尊重外键顺序，测试数据必须隔离；
- 生成 ORM 代码后检查变更范围，避免覆盖手写扩展。

## 12. 异步、消息和外部服务

### 12.1 异步行为

异步测试必须使用“最终一致性断言”：

```text
触发动作 → 在有限超时内轮询可观察结果 → 满足断言即结束
```

不得使用固定长时间休眠猜测任务何时完成。轮询必须有：

- 明确的最大超时；
- 合理的轮询间隔；
- 最终对业务结果的断言；
- 超时后的可诊断错误信息。

### 12.2 外部服务

- 默认测试套件不得依赖公网服务。
- 单元测试使用 fake、stub 或 mock 隔离第三方边界。
- 集成测试优先使用本地模拟服务器、容器或沙箱。
- 确需访问真实外部服务的测试必须使用明确标签，例如 `online`，并从默认快速测试中排除。
- 标签必须反映测试的真实依赖；已经完全 mock 的测试不应继续标记为在线测试。
- 测试密钥不得写入仓库、日志或断言快照。

## 13. 测试代码规范

### 13.1 命名

沿用项目既有风格。项目没有明确风格时，默认采用：

```text
测试类：<业务能力或动作>Test
测试方法：<条件>_<行为>_<预期>
```

示例：

```text
guest_cannot_create_question
authenticated_user_can_create_question
title_is_required
owner_can_update_own_resource
user_cannot_update_another_users_resource
```

名称必须描述业务结果，不写 `testMethod1`、`works`、`successCase` 等无信息名称。

### 13.2 Given–When–Then

```text
given：只准备本场景所需状态
when：只执行一个主要动作
then：断言对外结果和必要副作用
```

一个测试可以有多个相关断言，但应只有一个失败原因主题。

### 13.3 断言

- 断言具体业务结果，不只断言“非空”或 HTTP 成功。
- API 测试同时检查适用的状态码、响应结构和关键数据。
- 写操作至少验证一个持久化或事件副作用。
- 异常测试验证异常类型；消息稳定且属于契约时再验证消息。
- 集合测试在顺序属于契约时验证顺序，否则使用无序断言。
- 时间、随机值和 ID 使用范围、格式或关系断言，避免脆弱的精确值。

### 13.4 测试数据

- 使用 Factory、Builder、Fixture 或 Seeder 表达数据意图。
- 提供合理默认值，单个测试只覆盖与场景有关的字段。
- “构造但不持久化”和“构造并持久化”应有清晰区别。
- 测试数据默认唯一，避免并行执行时冲突。
- 不依赖其他测试创建的数据。
- 公共测试数据工具自身应保持简单；复杂逻辑需要测试。

### 13.5 隔离与清理

- 每个测试必须可单独运行、重复运行、乱序运行。
- 优先使用事务回滚、独立 schema/数据库或可靠清理。
- 复用容器可以加速，但不得复用测试业务状态。
- 文件测试使用临时目录，并在测试后清理。
- 邮件、消息和缓存状态必须在场景间重置。
- 不得通过给测试排序来解决状态污染。

### 13.6 测试替身

- mock 外部边界和昂贵协作者，不 mock 值对象和简单领域对象。
- 不对 ORM、HTTP 客户端或框架写“自我实现式 mock”来假装集成测试。
- 只有交互本身属于契约时才验证调用次数。
- 优先验证状态或输出，避免测试私有方法和内部调用顺序。
- 重构频繁导致测试大量破坏时，应检查测试是否绑定了实现细节。

### 13.7 复用

相同契约作用于多种资源时，可以使用抽象契约测试，由子类只提供资源类型、路径或夹具。公共抽象必须让失败报告仍能定位具体资源。

## 14. AI 工作协议

### 14.1 开始实现前

AI 应向用户简要说明：

- 已识别的行为切片；
- 拟新增或修改的测试用例、测试层级和关键断言；
- 将使用的目标测试命令；
- 发现的环境或既有失败。

AI 必须等待开发者审核测试方案。未收到明确同意时，本步骤是阻塞门禁，不能自动继续编辑测试或实现生产代码。

### 14.2 循环中的证据

每个行为切片至少保留以下证据：

```text
行为：
测试方案：
开发者批准：
测试：
Red 命令：
Red 原因：
Green 命令：
Green 结果：
重构：
回归命令：
回归结果：
```

不要求把完整日志写入仓库，但最终报告必须能让用户知道 AI 实际运行了什么以及结果如何。

### 14.3 失败处理

测试失败时，AI 必须先分类：

1. 预期 Red；
2. 本次实现引入的回归；
3. 测试或夹具错误；
4. 环境/基础设施错误；
5. 与本任务无关的既有失败；
6. 疑似不稳定测试。

不得在未分类前盲目修改生产代码。

疑似不稳定测试必须诊断时间、随机性、共享状态、并发和外部依赖；不得仅靠多次重跑直到偶然通过。

### 14.4 无法运行测试

如果因权限、依赖服务、网络、许可证或硬件原因无法运行测试：

- AI 可以继续做只读诊断和准备测试代码；
- AI 不得声称测试已通过；
- AI 必须给出已尝试的命令、具体阻塞和用户可执行的下一步；
- 未获得 Red/Green 证据时，任务状态应标记为“未验证”，而不是“完成”。

### 14.5 变更范围

- 只修改当前任务需要的文件。
- 保留用户已有改动。
- 不主动提交、推送、创建分支或发布，除非用户明确授权。
- 发现邻近问题时先记录；只有它阻塞当前行为或用户授权时才扩大范围。

## 15. 完成门禁（Definition of Done）

只有以下条件全部满足，AI 才能宣布行为完成：

- [ ] 需求已拆成明确、可观察的行为。
- [ ] 测试方案已提交开发者并获得明确同意。
- [ ] 每个新增/修复行为都有对应自动化测试。
- [ ] AI 未在批准范围之外修改任何测试。
- [ ] 新测试曾因正确原因失败，并记录了 Red 证据。
- [ ] 目标测试已经通过。
- [ ] 受影响模块的回归测试已经通过。
- [ ] 风险允许时，完整测试、静态检查和格式检查已经通过。
- [ ] 测试覆盖成功路径及本次最重要的失败/权限/边界路径。
- [ ] 测试彼此隔离，不依赖顺序或本机遗留状态。
- [ ] 没有通过削弱测试、吞异常或硬编码结果来换取 Green。
- [ ] 数据库迁移、生成代码、配置和文档已按需同步。
- [ ] 未泄露密钥、个人数据或测试生成的二进制文件。
- [ ] 最终报告列出变更、测试命令、结果和任何未验证风险。

如果完整测试因耗时或环境限制未运行，AI 必须明确说“目标/模块测试通过，完整测试未运行”，不能笼统声称“所有测试通过”。

## 16. AI 最终报告模板

```markdown
已完成：<一句话描述用户可见结果>

TDD 证据：
- 测试审核：开发者已同意 `<测试方案或测试文件>`
- Red：`<命令>`，因 `<目标行为尚未实现/缺陷复现>` 失败
- Green：`<命令>`，`<N>` 个测试通过
- 回归：`<命令>`，结果 `<通过/存在既有失败>`

主要变更：
- <测试与行为>
- <最小实现>
- <必要重构、迁移或配置>

未验证项/风险：
- <没有则写“无”>
```

## 17. 常用执行清单

### 新功能

```text
[ ] 读规则、代码、测试和构建配置
[ ] 建立测试基线
[ ] 提交最小行为与测试方案
[ ] 获得开发者明确审核同意
[ ] 只按批准范围编写测试
[ ] 运行并确认有效 Red
[ ] 写最小实现
[ ] 运行并确认 Green
[ ] 重构并再次运行目标测试
[ ] 增加下一场景并重复循环
[ ] 模块回归、完整回归、静态检查
[ ] 按模板报告证据
```

### 缺陷修复

```text
[ ] 提交复现用户症状的回归测试方案
[ ] 获得开发者明确审核同意
[ ] 只按批准范围编写回归测试
[ ] 确认测试因该缺陷而 Red
[ ] 定位根因
[ ] 最小修复并 Green
[ ] 检查相邻边界
[ ] 运行回归
[ ] 报告复现证据和修复结果
```

### 纯重构

```text
[ ] 变更前测试 Green
[ ] 必要时提交表征测试方案并获得开发者同意
[ ] 只按批准范围增加表征测试
[ ] 小步调整结构
[ ] 每步保持 Green
[ ] 变更后运行相同或更大范围回归
[ ] 明确说明没有制造 Red
```

## 18. Spring Boot / Maven 项目适配示例

本节仅是适配示例，不是通用项目的强制命令。

```text
语言/框架：Java 17 / Spring Boot / Maven
单元测试目录：src/test/java/**/unit
集成测试目录：src/test/java/**/integration
测试数据工厂目录：src/test/java/**/factory
数据库迁移目录：src/main/resources/db/migration

TEST_CASE：mvn test -Dtest=<TestClass>#<test_method> -DexcludedGroups=online
TEST_FILE：mvn test -Dtest=<TestClass> -DexcludedGroups=online
TEST_FAST：mvn test -DexcludedGroups=online
TEST_ALL：mvn test
```

推荐模式：

- 单元测试使用 JUnit 5、AssertJ 和 Mockito；
- HTTP 集成测试使用 `@SpringBootTest` + MockMvc；
- MySQL、Kafka、Redis 等基础设施使用 Testcontainers；
- 测试配置通过动态属性注入，不读取开发环境连接；
- 认证场景使用测试安全上下文；
- 邮件使用本地测试服务器；
- 异步结果使用 Awaitility 有界轮询；
- 数据库结构使用只增不改的 Flyway 迁移；
- Factory 负责对象默认值，Seeder 负责按业务语义持久化测试数据。

## 19. 本规范的实践来源与迁移原则

本规范提炼自一个 Spring Boot TDD 项目的实际结构和历史：

- 测试按 `unit`、`integration`、`factory`、`matcher`、`listener` 分层；
- 集成测试通过公共基类启动 MySQL、Kafka、Redis 和测试邮件服务；
- API 行为使用 MockMvc 验证，策略和服务使用 Mockito 隔离；
- 测试采用业务句式命名和 Given–When–Then；
- 工厂与 Seeder 降低数据搭建噪音；
- 异步行为使用有限时间轮询；
- 公网测试使用标签与默认套件隔离；
- 权限缺陷通过集成回归测试和策略单元测试共同保护。

迁移到其他项目时，应保留的是工作原则和证据门禁，而不是照搬 Spring 注解、目录名、HTTP 状态约定或具体工具。先观察目标项目，再填写“项目适配区”。
